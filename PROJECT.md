# คู่มือโปรเจกต์ — เว็บสต็อก Alpha MDX

เอกสารสำหรับคนที่ต้องเข้ามาแก้ระบบนี้ (README.md เป็นขั้นตอนติดตั้งครั้งแรก ไฟล์นี้คือวิธีแก้และดูแลต่อ)

- **เว็บไซต์:** https://missfamealpha.github.io/stockalphamdx/
- **repo:** https://github.com/missfamealpha/stockalphamdx
- **ชีตต้นทาง:** Google Sheet id `18p59fU78rGJamuDsVmx5HXOMwzrEOW6qS0lLJv-iX44` แท็บแรก (gid 0)
- **คอลัมน์ที่ระบบใช้:** A = ชื่อสินค้า · B = คงเหลือ · C = หมายเหตุ (แถวแรกเป็นหัวตาราง)

---

## 1. ระบบทำงานยังไง

```
Google Sheet
    │  GitHub Actions (cron ทุกชั่วโมง 07:07–21:07 เวลาไทย)
    │  ดึง CSV ด้วย urllib ตรง ๆ ไม่ผ่านตัวกลาง
    ▼
build.py ──► data.json ──commit เข้า repo──► GitHub Pages
                                                  │
                                    index.html fetch data.json ตอนเปิดหน้า
```

สิ่งที่ต้องเข้าใจ 3 ข้อ:

1. **`index.html` ไม่มีข้อมูลอยู่ในตัว** เป็นแค่หน้าเปล่าที่ไป `fetch("data.json")` ตอนเปิด
   ดังนั้นแก้หน้าตาเว็บ = แก้ `index.html` / แก้ข้อมูล = แก้ที่ชีตแล้วรอซิงค์
2. **`data.json` เป็นไฟล์ที่เครื่องสร้าง** ห้ามแก้มือ เพราะรอบซิงค์ถัดไปจะเขียนทับทั้งไฟล์
3. **หน้าเว็บไม่อัปเดตเองระหว่างเปิดค้างไว้** ต้องกดปุ่ม "รีเฟรช" ในหน้า หรือโหลดหน้าใหม่

---

## 2. เตรียม VS Code (ครั้งเดียว)

ต้องมี **Git** กับ **Python 3** ในเครื่องก่อน

```bash
git --version        # ถ้าไม่มี: ดาวน์โหลดที่ git-scm.com
python3 --version    # macOS/Linux มีมาแล้ว · Windows ใช้ python --version
```

ตั้งชื่อผู้แก้ไข (ครั้งเดียวต่อเครื่อง):

```bash
git config --global user.name  "ชื่อคุณ"
git config --global user.email "sudruthai.c@alphamdx.com"
```

โคลน repo ลงเครื่องแล้วเปิดใน VS Code:

```bash
git clone https://github.com/missfamealpha/stockalphamdx.git
cd stockalphamdx
code .
```

ส่วนขยาย VS Code ที่ช่วยได้ (ไม่บังคับ): **Python**, **YAML**, **Live Server**

### ทุกครั้งที่จะเริ่มแก้ — ดึงของใหม่ลงมาก่อน (สำคัญมาก)

```bash
git pull
```

เพราะบอทซิงค์ commit `data.json` ทุกชั่วโมง ถ้าไม่ `git pull` ก่อน เวลา `git push` จะถูกปฏิเสธ
(`rejected — fetch first`) นี่คือปัญหาที่จะเจอบ่อยที่สุด

### แก้เสร็จแล้วส่งขึ้น

```bash
git add -A
git commit -m "บอกสั้น ๆ ว่าแก้อะไร"
git push
```

ใน VS Code ใช้แถบ **Source Control** (ไอคอนกิ่งไม้ด้านซ้าย) กดแทนก็ได้:
`Sync Changes` = pull + push, ช่องข้อความบนสุด = commit message

พอ push ขึ้นไป GitHub Pages จะ deploy ใหม่เองภายในไม่กี่นาที

---

## 3. ทดสอบในเครื่องก่อน push

**อย่าแก้แล้ว push เลย** ลองในเครื่องก่อนเร็วกว่าและไม่พาเว็บจริงล่ม

```bash
# 1. สร้าง data.json จากข้อมูลจำลอง (ไม่ต้องต่อชีต)
python3 build.py --csv sample.csv

# 2. หรือดึงชีตจริง (data.json ที่ได้จะทับของเดิมในเครื่อง ไม่กระทบเว็บจนกว่าจะ push)
python3 build.py

# 3. เปิดดูหน้าเว็บ
python3 -m http.server 8000
#    แล้วเปิด http://localhost:8000
```

เปิดด้วย `file://` ตรง ๆ **ไม่ได้** เพราะเบราว์เซอร์ห้าม `fetch` ไฟล์ในเครื่อง ต้องผ่าน http.server
(หรือใช้ส่วนขยาย Live Server กด "Go Live")

พอพอใจแล้วค่อย `git add -A && git commit && git push` — `data.json` ที่ push ขึ้นไปจะถูกรอบซิงค์
ถัดไปเขียนทับอยู่ดี ไม่ต้องกังวลว่าจะค้างข้อมูลทดสอบ

---

## 4. งานแก้ที่เจอบ่อย

### เปลี่ยนความถี่การซิงค์

แก้ `.github/workflows/sync.yml` บรรทัด `- cron:` (**ชั่วโมงเป็น UTC เสมอ ไทย = UTC+7**)

| อยากได้ | ค่า cron |
|---|---|
| ทุกชั่วโมง 07:07–21:07 ไทย (ค่าปัจจุบัน) | `"7 0-14 * * *"` |
| ทุกชั่วโมง 24 ชม. | `"7 * * * *"` |
| ทุก 30 นาที 07:00–21:00 ไทย | `"7,37 0-14 * * *"` |
| ทุก 2 ชั่วโมง 07:00–21:00 ไทย | `"7 0-14/2 * * *"` |
| ทุกชั่วโมง จันทร์–ศุกร์ | `"7 0-14 * * 1-5"` |

### เพิ่ม/ย้ายหมวดหมู่สินค้า

อยู่ในตัวแปร `CATS` ใน `build.py` แต่ละหมวดคือ `("รหัส", "ชื่อที่แสดง", [คำค้น...])`
ระบบเอาชื่อสินค้ามาแปลงเป็นตัวพิมพ์เล็กแล้วหาว่ามีคำค้นไหนอยู่ในชื่อ — ถ้าไม่ตรงเลยจะไปหมวด `other`

เช่นอยากให้ของที่ชื่อมีคำว่า "ethanol" ไปอยู่หมวดวัสดุสิ้นเปลือง ก็เติมคำเข้าไปในลิสต์ของ `consum`:

```python
 ("consum", "วัสดุสิ้นเปลือง", [
   "ถุงมือ","gloves","ethanol",        # <-- เติมที่นี่
   ...
```

**ลำดับสำคัญ:** ถ้าชื่อสินค้าตรงกับคำค้นของหลายหมวด ระบบจะเลือกตามลำดับในตัวแปร `PRIORITY`
(`admin` → `service` → `device` → `consum` → `hema` → `rapid` → `chem`) เช่น
"เครื่องเป่าแอลกอฮอล์" ตรงทั้ง `device` และ `consum` แต่ `device` มาก่อนจึงชนะ

เพิ่มหมวดใหม่ทั้งหมวด ต้องแก้ 3 ที่: `CATS`, `ORDER` (ลำดับที่แสดงบนเว็บ) และถ้าไม่ใช่ของในคลัง
ก็ใส่ใน `NONSTOCK` ด้วย

### จัดการชื่อสินค้าที่ซ้ำกันในชีต

ถ้าเจอของชิ้นเดียวกันแต่สะกดต่างกันสองแถว ใส่คู่ชื่อลงใน `DUP_GROUPS` ใน `build.py`
เว็บจะขึ้นเตือนในพาเนลท้ายหน้า แต่**ไม่รวมยอดให้** (เพราะไม่รู้ว่าอันไหนถูก)
วิธีแก้ที่ถูกต้องจริง ๆ คือไปรวมแถวในชีตต้นทาง

### เปลี่ยนเกณฑ์ "เหลือน้อย"

ตอนนี้ 0 = หมด, 1–2 = เหลือน้อย, 3+ = มีของพอ เกณฑ์นี้อยู่ใน `index.html` ฟังก์ชัน `statOf()`
และมีตัวเลข `<= 2` ซ้ำอีกไม่กี่จุด (`matchStat`, `setCallout`, `buildChips`) แก้ต้องแก้ให้ครบทุกจุด

### เปลี่ยนชีตต้นทาง

แก้ `SHEET_ID` / `GID` ที่หัวไฟล์ `build.py` — ชีตต้องเปิดให้ "ผู้ที่มีลิงก์" ดูได้ ไม่งั้นดึงไม่ออก

### เปลี่ยนหน้าตาเว็บ

ทุกอย่างอยู่ใน `index.html` ไฟล์เดียว: สี/ฟอนต์อยู่ในตัวแปร CSS ที่ `:root` บนสุด
(มีชุดสำหรับ dark mode แยกอีกสองบล็อกด้านล่าง ต้องแก้ให้ตรงกันทั้งสามที่)
ข้อความท้ายหน้าอยู่ในแท็ก `<footer>`

---

## 5. เวลาระบบพัง

### workflow ขึ้นแดง

ไปดูที่ https://github.com/missfamealpha/stockalphamdx/actions → คลิกรอบที่แดง → กดดู log ของ step ที่มี ✗

| ข้อความใน log | สาเหตุ | วิธีแก้ |
|---|---|---|
| `ล้มเหลว: ได้ N แถว ... ต้องได้อย่างน้อย 100` | ดึงชีตไม่ครบ หรือชีตถูกปิดการแชร์ | เช็คว่าชีตยังเปิดให้ผู้ที่มีลิงก์ดูได้ แล้วกด Re-run |
| `ล้มเหลว: รายการลดจาก A เหลือ B (เกิน 20%)` | ของในชีตหายไปเยอะผิดปกติ | ถ้าลบจริงตามนั้น ให้รัน `python3 build.py --force` ในเครื่องแล้ว commit เอง |
| `Permission to ... denied` / `403` ตอน push | สิทธิ์ Actions ไม่พอ | Settings → Actions → General → Workflow permissions = **Read and write** |
| `HTTP Error 404` ตอนดึงชีต | `SHEET_ID`/`GID` ผิด | เช็คค่าที่หัว `build.py` |

**ข้อดีของ guard:** เวลาพัง `data.json` เดิมยังอยู่ครบ เว็บจึงยังแสดงข้อมูลรอบก่อนต่อไปได้
ไม่มีทางที่ข้อมูลพัง ๆ จะขึ้นเว็บ

### เว็บขึ้น "ยังไม่มีข้อมูล"

แปลว่าหน้าเว็บโหลด `data.json` ไม่ได้ — เช็คว่าไฟล์ยังอยู่ใน repo ไหม
(https://missfamealpha.github.io/stockalphamdx/data.json ต้องเปิดเจอ JSON)

### ตัวเลขบนเว็บไม่ตรงกับชีต

ปกติ — เว็บช้ากว่าชีตได้ถึงราว 1 ชั่วโมงกว่า (รอรอบ cron + CDN ของ Pages)
กดปุ่ม "รีเฟรช" ในหน้าเว็บก่อน ถ้ายังไม่ตรงให้ไปสั่ง Run workflow เองที่แท็บ Actions

### cron ไม่ยิงตามเวลา

GitHub ไม่การันตีเวลาเป๊ะ คลาดไปหลายนาทีเป็นเรื่องปกติ
และ scheduled workflow จะถูกปิดอัตโนมัติถ้า repo ไม่มีความเคลื่อนไหว 60 วัน —
แต่ workflow นี้ commit เองทุกชั่วโมงจึงถือว่ามีความเคลื่อนไหวเสมอ

---

## 6. เรื่องที่ควรรู้ไว้

- **repo เป็น public** ใครที่รู้ URL อ่านโค้ดและตัวเลขสต็อกทั้งหมดได้
  ถ้าต้องปิด: GitHub Pages บน repo private ต้องใช้แพลนจ่ายเงิน หรือย้ายไป Cloudflare Pages (ฟรี)
- **`.gitignore` ตกไปตอนอัปโหลดครั้งแรก** ถ้ายังไม่มีในเครื่อง สร้างไฟล์ชื่อ `.gitignore` ใส่:
  ```
  __pycache__/
  *.pyc
  .DS_Store
  ```
- **ไฟล์ `download`** ที่อยู่ใน repo เป็นขยะที่ติดมาตอนอัปโหลด ลบทิ้งได้
- **ระบบเดิมที่เลิกใช้แล้ว:** แอปเวอร์ชัน artifact ใน Claude
  (`claude.ai/code/artifact/752f88b2-1c1c-48e2-87ea-77b55edf20e6`) ยังเปิดดูได้แต่ข้อมูลค้างที่
  9 ก.ย. 2026 และ scheduled task ที่เคยซิงค์ให้ถูกปิดไปแล้ว
- **วิธีเดิมตกแถวเยอะ** รอบแรกของระบบใหม่ได้ 252 รายการ ขณะที่ระบบเดิมบันทึกไว้ 193 แถว
  เพราะเดิมให้โมเดลอ่านชีตแล้วถอดข้อความ ซึ่งตกแถว ตอนนี้ดึงไฟล์ดิบจึงได้ครบ
